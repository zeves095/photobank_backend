let NOTIFICATION_OVERLAY = '#notification-overlay';
let COMPONENT_HEADER = '.component-header';
let COMPONENT_TITLE = '.component-title';
let COMPONENT_TOP_SECTION = '.component-body__top-section';
let BUTTON_BLOCK = '.button-block';
let HELP_TEXT = '.help-text';

let LINK_LIST_COMPONENT = 'Connect(LinkList)';
let LINK_MANAGER_COMPONENT = 'Connect(LinkManager)';
let LINK_ADDER_COMPONENT = 'Connect(LinkAdder)';
let LINK_ADD_FORM_COMPONENT = 'Connect(LinkAddForm)';
let LINK_RESOURCE_COMPONENT = 'Connect(LinkResource)';
let RESOURCE_EXPLORER_COMPONENT = 'Connect(ResourceExplorer)';
let RESOURCE_SEARCH_FORM_COMPONENT = 'Connect(ResourceSearchForm)';
let RESOURCE_SEARCH_RESULTS_COMPONENT = 'Connect(ResourceSearchResults)';
let FORM_WRAPPER_COMPONENT = 'Connect(FormWrapper)';
let FORM_SUBCOMPONENT = 'Form';

let LINK_MANAGER_WRAPPER = '.link-manager';
let LINK_MANAGER_WRAPPER_WRAPPER = '#link-manager';
let RESOURCE_EXPLORER_WRAPPER = '.resource-explorer';
let LINK_LIST_WRAPPER = '.link-list';
let LINK_RESOURCE_WRAPPER = '.link-resource';
let RESOURCE_SEARCH_FORM_WRAPPER = '.resource-search-form';
let RESOURCE_SEARCH_RESULTS_WRAPPER = '.resource-search-results';
let LINK_LIST_ITEM = '.link-list div[data-linkid]';
let LINK_ADDER_WRAPPER = '.link-adder';
let LINK_ADD_FORM_WRAPPER = '.link-add-form';
let RESOURCE_LIST_ITEM = '.resource.list-item';
let RESOURCE_LIST_ITEM_SELECTED = '.resource.list-item.selected';
let LINK_TARGETS = '.link-list__tabs';

let FORM_WRAPPER_WRAPPER = '.form-wrapper';

export let selectors = {
  NOTIFICATION_OVERLAY,
  COMPONENT_HEADER,
  COMPONENT_TITLE,
  COMPONENT_TOP_SECTION,
  BUTTON_BLOCK,
  HELP_TEXT,
  components:{
    LINK_LIST_COMPONENT,
    LINK_MANAGER_COMPONENT,
    LINK_ADDER_COMPONENT,
    LINK_ADD_FORM_COMPONENT,
    LINK_RESOURCE_COMPONENT,
    RESOURCE_EXPLORER_COMPONENT,
    LINK_ADD_FORM_COMPONENT,
    FORM_WRAPPER_COMPONENT,
    RESOURCE_SEARCH_FORM_COMPONENT,
    RESOURCE_SEARCH_RESULTS_COMPONENT,
    FORM_SUBCOMPONENT,
  },
  linkmanager:{
    LINK_MANAGER_WRAPPER,
    LINK_MANAGER_WRAPPER_WRAPPER,
    LINK_LIST_WRAPPER,
    LINK_RESOURCE_WRAPPER,
    LINK_LIST_ITEM,
    LINK_ADDER_WRAPPER,
    LINK_ADD_FORM_WRAPPER,
    RESOURCE_LIST_ITEM,
    RESOURCE_LIST_ITEM_SELECTED,
    RESOURCE_EXPLORER_WRAPPER,
    RESOURCE_SEARCH_FORM_WRAPPER,
    RESOURCE_SEARCH_RESULTS_WRAPPER,
    LINK_TARGETS,
  },
  common:{
    FORM_WRAPPER_WRAPPER,
  },
  upload:{

  }
}
